
export default function RolesSection() {
  // const { data, isPending, isError, error } = useGetAllRoles();

  // if (isPending) {
  //   return <LoadingSpinner message="Loading roles..." />;
  // }

  return (
    <div>
      <h1>Roles Section</h1>
    </div>
  );
}